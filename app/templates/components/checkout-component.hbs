{{#each cart as |unit|}}
  <p>{{unit.item}} - {{unit.price}}</p>
  <img class="item-image" src={{unit.image}}>
{{/each}}
{{free-shipping totalPrice}}
<p>Items: {{itemCount}}</p>
<p>Total: {{totalPrice}}</p>

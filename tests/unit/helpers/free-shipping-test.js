import { freeShipping } from 'coffee-store/helpers/free-shipping';
import { module, test } from 'qunit';

module('Unit | Helper | free shipping');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = freeShipping([42]);
  assert.ok(result);
});
